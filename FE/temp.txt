import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    host: true,
    port: 5173,
    watch: {
      usePolling: true
    },
    // CORS 및 허용된 호스트 설정
    cors: true,
    strictPort: true,
    // i12b212.p.ssafy.io 호스트 허용
    hmr: {
      clientPort: 5173
    }
  }
})